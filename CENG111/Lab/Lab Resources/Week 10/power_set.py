# Using the append_element function write a recursive function called "power_set" which takes a set as a Python list and returns its power set. (The order of the elements in the returned list is not important.)

from append_element import append_element

# Try to solve this problem by yourself. The solution will be shared later.
