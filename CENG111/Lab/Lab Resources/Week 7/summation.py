# This line is a comment and, thus, is ignored by the interpreter
a = input('Enter the first number: ') # a comment at the end of the line
b = input('Enter the second number: ')
print(int(a) + int(b))
